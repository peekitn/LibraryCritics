<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Verificação de Código - Minha Biblioteca</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    /* Tela centralizada */
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background: linear-gradient(135deg, #e0e7ff, #f8fafc);
      font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }

    .verify-card {
      background: #ffffff;
      padding: 40px 30px;
      border-radius: 20px;
      box-shadow: 0 12px 40px rgba(0,0,0,0.08);
      width: 100%;
      max-width: 400px;
      text-align: center;
    }

    .verify-card h1 {
      font-size: 2rem;
      margin-bottom: 25px;
      color: #1e293b;
    }

    .verify-card input {
      width: 100%;
      padding: 14px 16px;
      margin-bottom: 18px;
      border-radius: 12px;
      border: 1px solid #cbd5e1;
      font-size: 1rem;
      outline: none;
      transition: border-color 0.2s, box-shadow 0.2s;
    }

    .verify-card input:focus {
      border-color: #6366f1;
      box-shadow: 0 0 0 4px rgba(99,102,241,0.1);
    }

    .verify-card .button {
      width: 100%;
      padding: 12px;
      font-size: 1rem;
      font-weight: 600;
      border-radius: 12px;
      border: none;
      cursor: pointer;
      transition: all 0.2s ease;
      box-shadow: 0 6px 18px rgba(99,102,241,0.25);
    }

    .verify-card .button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(99,102,241,0.35);
    }

    .verify-card .error {
      color: #ef4444;
      margin-bottom: 15px;
      font-weight: 500;
    }

    .verify-footer {
      margin-top: 20px;
      font-size: 0.9rem;
      color: #475569;
    }

    .verify-footer a {
      background: #4ade80;
      color: white;
      padding: 10px 18px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      display: inline-block;
      margin-top: 8px;
      transition: all 0.2s ease;
      box-shadow: 0 6px 15px rgba(74,222,128,0.25);
    }

    .verify-footer a:hover {
      transform: translateY(-1px);
      box-shadow: 0 10px 25px rgba(74,222,128,0.35);
    }

    .code-note {
      font-size: 0.85rem;
      color: #64748b;
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <div class="verify-card">
    <h1>📧 Verifique seu E-mail</h1>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <p class="code-note">Digite o código de 6 dígitos enviado para <strong><%= email %></strong></p>

    <form method="POST" action="/verify">
      <input type="hidden" name="username" value="<%= username %>">
      <input type="hidden" name="email" value="<%= email %>">
      <input type="hidden" name="password" value="<%= password %>">
      <input name="code" placeholder="Código de verificação" required>
      <button class="button" type="submit">Verificar</button>
    </form>

    <div class="verify-footer">
      <p>Já tem conta?</p>
      <a href="/login">Login</a>
    </div>
  </div>
</body>
</html>
